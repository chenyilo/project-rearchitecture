# 目标模型：<项目名>

> 本项目后续所有改动的**蓝图**。**必须先经用户确认，才能进入阶段 3。**

- 生成日期：
- 依据：`GLOSSARY.md` / `SEMANTIC-MAP.md` / `DRIFT-REPORT.md`
- 状态：草稿 / **已确认（用户签字）**

---

## 1. 目标分层选择

| 项 | 内容 |
|---|---|
| **选择的目标** | 不引入分层 / 轻量三层 / Clean Architecture / 按上下文拆模块 |
| **选择理由** | 基于项目实际情况论证，不要抄模板 |
| **明确排除的方案** | 例如：不分拆为独立服务（超出重构范围） |

---

## 2. 限界上下文清单

### BC1：<上下文名>

| 项 | 内容 |
|---|---|
| **职责** | 一句话说清这个上下文负责什么 |
| **包含的概念** | 引用 `GLOSSARY.md` 中的概念名 |
| **负责的契约点** | API 路径 / 表 / 事件 / 错误码 |
| **对应现有代码** | 现有文件与目录清单 |
| **域类型** | 核心域 / 支撑域 / 通用域 |
| **优先级** | 高 / 中 / 低 |

（按此结构逐个列出全部上下文）

---

## 3. 上下文依赖图

```
# 必须是**有向无环图**。文字描述 + ASCII 图都给出。

  BC1 ──► BC2 ──► BC4
   │               ▲
   └────► BC3 ─────┘
```

### 环检测

- [ ] 依赖图无环
- [ ] 核心域不依赖支撑域/通用域的实现细节（只依赖接口）
- [ ] 通用域不依赖业务域

> 若发现环，说明边界切错。回到 [domain-extraction.md](../references/domain-extraction.md)
> §2A.2 重新评估内聚判据，**不要用「打破环」的临时依赖倒置来掩盖错误的边界**。

---

## 4. 契约点归属

| 目标模块 | API 路径 | 表 | 事件/topic | 错误码 |
|---|---|---|---|---|
| BC1 | | | | |

> 检查：每个契约点**恰好**归属一个模块。
> 有契约点无法归属 → 回到阶段 1 检查是否有未识别的概念（D5）。

---

## 5. 目标模块 → 现有代码映射

> 这是阶段 3 / 阶段 4 的施工图。不允许出现「以后再说」的孤儿文件。

| 目标模块 | 现有文件/目录 | 处置 | 备注 |
|---|---|---|---|
| BC1 | `src/orders/*` | 迁入 | |
| — | `src/utils/misc.ts` | 拆分到 BC1/BC2 | 见 DRIFT #12 |
| — | `src/legacy/old_flow.py` | 待删除（幽灵概念） | 见 DRIFT #7，独立批次 |

---

## 6. 漂移项归宿

> 阶段 1 的每条漂移必须在此找到归宿：改名、迁移、或**明确不处理并说明理由**。

| DRIFT # | 分类 | 归宿 | 所属批次 |
|---|---|---|---|
| 1 | D2 同义不同名 | 统一为 `Customer` | 批次 1 |
| 7 | D4 幽灵概念 | 删除 | 批次 4 |
| 15 | D3 名实不符 | 先拆副作用，后改名 | 批次 2 + 3 |

---

## 7. 不处理范围

> 明确列出本次**不动**的东西。尤其包括阶段 0 护栏中无基线覆盖的区域。

| 区域 | 原因 | 后续处置 |
|---|---|---|
| | 无行为基线覆盖 | 待补基线后另开一轮 |

---

## 8. 用户确认

| 项 | 内容 |
|---|---|
| 确认人 | |
| 确认方式 | |
| 确认结论 | |
| 附带条件 | |
