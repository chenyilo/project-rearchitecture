---
project: <项目名>
phase: <0 | 1 | 2 | 3 | 4>
current_slice: <切片标识，如 S3，或 `-`>
current_batch: <批次标识，如 B2，或 `-`>
last_updated: <YYYY-MM-DD>
baseline_status: <green | known-failing | red | unknown>
---

# 重架构全局状态：<项目名>

> 本文件由 `project-rearchitecture` 技能维护，是**跨会话恢复的唯一入口**。
> 每次会话开始先读本文件，结束时必须更新本文件。
> 保持简短：细节请下沉到 `AUDIT-PROGRESS.md` / `DRIFT-REPORT.md`，本文件目标 1–2 屏。

---

## 1. 当前阶段

| 项 | 内容 |
|---|---|
| 当前阶段 | 阶段 <编号>：<行为冻结 / 语义审计 / 目标模型与命名归一 / 架构重划 / 分批迁移> |
| 阶段目标 | <这一阶段要产出什么> |
| 阶段完成判据 | <可检验的判据，例如「UNKNOWN 收敛为 0 且 GLOSSARY 冻结」> |
| 距完成还差 | <具体缺口> |
| 阻塞项 | <无 / 具体阻塞原因> |

### 阶段看板

| 阶段 | 状态 | 完成判据 | 阻塞项 |
|---|---|---|---|
| 阶段 0 行为冻结 | ⬜ | 基线一条命令可复现，`UNKNOWN` 已列出 | |
| 阶段 1 语义审计 | ⬜ | <N> 个切片全部有证据记录，漂移全部评分 | |
| 阶段 2 目标模型 | ⬜ | `UNKNOWN` 收敛为 0，`GLOSSARY.md` 冻结 | |
| 阶段 3 架构重划 | ⬜ | `TARGET-MODEL.md` 与模块边界确认 | |
| 阶段 4 分批迁移 | ⬜ | 批次全部收尾，基线全绿 | |

> 状态取值只用：`⬜ 未开始` / `🔄 进行中` / `✅ 已完成` / `⛔ 受阻`。

---

## 2. 当前批次

| 项 | 内容 |
|---|---|
| 批次号 | <B<n>> |
| 批次类型 | 命名归一 / 结构重划 / 删除幽灵概念 / 引入概念 / 迁移 |
| 批次目标 | <一句话，必须是单一语义主题> |
| 涉及切片 | <S2, S3> |
| 涉及漂移项 | <DRIFT-REPORT.md 中的编号，如 D2-07, D1-02> |
| 分支 / 工作树 | <分支名或 worktree 路径> |
| 起始 commit | <短哈希> |
| 回滚点 | <短哈希，必须有效；必要时用 `git tag rearch-rollback-B<n> <commit>` 固定> |
| 当前进展 | <做到哪一步：改名映射已确认 / 代码改到第 3 个文件 / 测试已跑> |
| 是否涉及破坏性变更 | 否 / 是 → 四件套状态：版本< > 迁移脚本< > 弃用期< > 回滚步骤< > |

### 本批次文件级进展

| # | 文件 | 动作 | 状态 |
|---|---|---|---|
| 1 | `<path>:<line>` | 改名 / 拆结构 / 删除 | ✅ 已完成 |
| 2 | `<path>:<line>` | | 🔄 进行中 |
| 3 | `<path>:<line>` | | ⬜ 未开始 |

---

## 3. 基线状态

| 项 | 值 |
|---|---|
| 基线命令 | `<从 BEHAVIOR-CONTRACT.md 抄，不要凭记忆写>` |
| 子集基线命令 | `<每次编辑后运行>` |
| 本会话开始时的状态 | green / known-failing / red |
| 最近一次全量运行 | <YYYY-MM-DD>，结果：<通过 X / 失败 Y> |
| 已知失败项 | 见 `BEHAVIOR-CONTRACT.md` §已知失败清单（`KNOWN-FAILING`） |
| 非确定性项 | <数量>，见 `BEHAVIOR-CONTRACT.md` |
| 无基线覆盖（护栏）区域 | <列出，这些区域本次不得改动> |
| 基线耗时 | <全量 X 分钟 / 子集 Y 秒> |

> [!WARNING]
> 基线不是「改完再跑」的参考，而是**门禁**。基线红即回滚当前批次，不要往前修。

---

## 4. 切片总览

> 详细记录见 `AUDIT-PROGRESS.md`。此处只保留汇总与状态，不写逐文件细节。

| 切片 | 范围 | 文件数 | 审计状态 | 处理状态 | 产出文件 |
|---|---|---|---|---|---|
| S1 | `<src/auth/>` | <12> | ✅ 已完成 | 🔄 批次 B2 中 | `GLOSSARY.md` §核心域 |
| S2 | `<src/order/>` | <19> | ✅ 已完成 | ⬜ 未开始 | `GLOSSARY.md` §支撑域 |
| S3 | `<src/billing/>` | <8> | 🔄 进行中 | ⬜ 未开始 | — |
| S4 | `<src/payment/>` | <17> | ⬜ 未开始 | ⬜ 未开始 | — |
| S5 | `<src/notify/>` | <6> | ⬜ 未开始 | ⬜ 未开始 | — |

**汇总**：切片总数 <5>；已审计 <2>；`UNRESOLVED` 合计 <3>；预计剩余 <3> 次会话。

---

## 5. 已完成批次记录

> 已完成／已回滚的批次都要记。**回滚记录比成功记录更值钱**，不要删。

| 批次 | 目标 | 结果 | 起始 commit | 收尾 commit | 基线 | 备注 |
|---|---|---|---|---|---|---|
| B0 | 建立行为基线（阶段 0） | ✅ 完成 | `<hash>` | `<hash>` | green | 基线命令见 BEHAVIOR-CONTRACT.md |
| B1 | `<把 customer/account 统一为 customer>` | ✅ 完成 | `<hash>` | `<hash>` | green | 改 <8> 个文件 |
| B2 | `<订单模块命名归一>` | ⛔ 已回滚 | `<hash>` | 回滚至 `<hash>` | red | 原因见 §7 |

### 失败批次详情（回滚时必须填写）

```markdown
### 失败批次：<B2>

- 日期：<YYYY-MM-DD>
- 目标：<批次原本要做什么>
- 失败点：<改到哪个文件／哪一步时失败>
- 现象：<具体错误输出，一行即可>
- 判定：<回归 / 基线未覆盖 / 漏改动态引用 / 环境问题>
- 已执行：`git reset --hard <commit>`，工作区<干净/不干净>
- **不要再试的方向**：<必须填写。例如「不要直接改 DB 列名——外部报表系统按名字读取」>
- 下次的替代方案：<例如「先加适配层读取，下个版本再契约化列名」>
```

---

## 6. 待办与优先级

> 排序依据：漂移评分（见 `DRIFT-REPORT.md` §评分）+ 契约处置（`KEEP` 高分项要先设计迁移方案）。

| 优先级 | 待办项 | 类型 | 依赖 | 预计会话数 | 备注 |
|---|---|---|---|---|---|
| P0 | <审计切片 S3> | 审计 | 无 | 1 | 阶段 1 未完成，阻塞阶段 2 |
| P0 | <D1-01 `status` 拆分> | 命名归一 | S3 审计完成 | 1 | Blocker（总分 9） |
| P1 | <D5-03 提取「会员等级」值对象> | 引入概念 | GLOSSARY 冻结 | 2 | 结构改动，风险高于改名 |
| P2 | <D4-02 删除废弃 feature flag 分支> | 删除 | 全量搜索引用 | 1 | 单独批次，不与其他改动混合 |

---

## 7. 已知风险与坑

> 每条都要写「为什么」和「该怎么做」。**只写结论的风险条目等于没写。**

| # | 风险 / 坑 | 影响 | 应对 |
|---|---|---|---|
| 1 | `<src/billing/` 下的测试是跟着实现写的，改了实现它会一起变绿>` | 该目录的绿灯**不能**作为功能未变的证据 | 该区域靠黄金样本比对，不靠单测 |
| 2 | `<DB 列 cust_id 被外部报表系统按名字读取>` | 改名会静默打断报表 | 该列标 `KEEP`，只统一内部命名 |
| 3 | `<基线全量运行需要 8 分钟>` | 每批次收尾会拖慢节奏 | 已建分层运行方案：批次内跑子集，收尾跑全量 |
| 4 | <...> | | |

---

## 8. 未决问题

| 指标 | 当前值 | 目标 | 走向是否健康 |
|---|---|---|---|
| `UNKNOWN`（契约点未判定 KEEP/BREAK-PLANNED） | <4> | 0（阶段 2 结束前） | 递减 |
| `UNRESOLVED`（无法判定语义的概念/漂移） | <3> | 每条均有处置 | 递减 |
| 未审计切片数 | <3> | 0 | 递减 |
| 漂移未处置项 | <12> | 0 | 递减 |

> [!IMPORTANT]
> 计数**只增不减**时必须在此处写明原因与收敛计划。
> `UNKNOWN` 与 `UNRESOLVED` 都不允许静默丢弃。

### 未决项明细

| # | 项 | 类型 | 为什么未决 | 处置 | 负责人 | 期限 |
|---|---|---|---|---|---|---|
| 1 | `tray` | `UNRESOLVED` | 证据不足，无法判定是别名还是独立概念 | 问用户 | <-> | <-> |
| 2 | `<billing 的 quota 字段>` | `UNKNOWN` | 契约处置未定 | 阶段 2 结束前收敛 | <-> | <-> |

---

## 9. 下一步入口

> [!CAUTION]
> 这一节必须**具体到文件与批次**。任何「继续审计」「处理剩下的」这类写法都是无效状态。
> 判定标准：一个完全没有上下文的 agent，只读这一节就能立刻开始工作。

**下次会话第一件事：**

```
<例：审计切片 S3 `src/billing/`（8 个文件），从未读的 `refundHandler.ts` 开始；
读完产出 GLOSSARY.md 的候选条目，不改任何业务代码。>
```

**紧接着：**

```
<例：把 S3 结论并入 SEMANTIC-MAP.md，更新 AUDIT-PROGRESS.md 的 S3 行状态为 ✅。>
```

**本次会话请勿触碰：**

```
<例：`src/billing/` 下的改动一律不做——该区域无基线覆盖（见 BEHAVIOR-CONTRACT.md §护栏）。>
```

---

## 10. 变更历史

| 日期 | 会话目标 | 结果 | 产出 / 状态变化 |
|---|---|---|---|
| <YYYY-MM-DD> | <建立行为基线> | ✅ 完成 | `BEHAVIOR-CONTRACT.md` 落盘；阶段 0 → 阶段 1 |
| <YYYY-MM-DD> | <审计切片 S1、S2> | ✅ 完成 | `GLOSSARY.md` §核心域/§支撑域；已审计 0 → 2 |
| <YYYY-MM-DD> | <批次 B2 订单命名归一> | ⛔ 回滚 | 见 §7 失败批次详情；漂移未处置 13 → 12 |
| <YYYY-MM-DD> | <因 rebase 修正 commit 引用> | ✅ 完成 | §2/§5 的哈希已逐条核对更新 |

---

## 11. 会话交接清单

```markdown
- [ ] 「当前批次」已更新到真实进展（不是计划，是已做完的部分）
- [ ] 「下一步入口」已写成可执行的具体动作（含文件路径与命令）
- [ ] `AUDIT-PROGRESS.md` 已反映本次完成的切片与产出文件
- [ ] 行为基线已重跑，结果与 `BEHAVIOR-CONTRACT.md` 一致
- [ ] `UNKNOWN` / `UNRESOLVED` / 未审计切片数 / 漂移进度 已更新（§8）
- [ ] 本次踩的坑已写进 §7，含「不要再试的方向」
- [ ] frontmatter 的 `phase` / `current_slice` / `current_batch` / `last_updated` / `baseline_status` 已更新
- [ ] 状态文件与代码改动已提交，提交信息带批次号（如 `rearch(B2): ...`）
- [ ] 工作区干净，或遗留改动已在本文件明确声明
- [ ] 若发生 rebase：commit 引用已修正，关键回滚点已用 `git tag` 固定
```
